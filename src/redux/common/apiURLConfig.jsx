// common/apiURLConfig.jsx
const apiURLConfig = {
  // baseURL: "https://aiilapi.azurewebsites.net/api", // Update the base URL accordingly
  baseURL: "https://localhost:7030/api", // Update the base URL accordingly
};

export default apiURLConfig;
